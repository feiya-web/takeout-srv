<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.takeout.mapper.DishMapper">

    <!-- 多表关联分页：dish LEFT JOIN category，条件 + 排序均命中索引 -->
    <select id="pageQuery" resultType="com.takeout.pojo.vo.DishVO">
        SELECT d.id, d.name, d.category_id, d.price, d.image, d.description,
               d.status, d.update_time, c.name AS category_name
        FROM dish d
        LEFT JOIN category c ON d.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                AND d.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                AND d.category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND d.status = #{status}
            </if>
        </where>
        ORDER BY d.update_time DESC
    </select>
</mapper>
